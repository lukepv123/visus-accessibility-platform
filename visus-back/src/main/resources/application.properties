spring.application.name=tcc-api

# Datasource MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/visusdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=visus
spring.datasource.password=${BD_PASSWORD:^85Q!ojge4s}

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Habilita o console web do H2
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# Secret do JWT Secret - VARI�VEL DE AMBIENTE
api.security.token.secret=${JWT_SECRET:I,9a#6RnO1YI}

# Configura��es do JavaMailSender
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_ACCOUNT:bluds.appflowy@gmail.com}
spring.mail.password=${APP_PASSWORD:hwej fguk kohy edui}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configura��es principais do Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html

# Cache (desativado em desenvolvimento)
spring.thymeleaf.cache=false

# Configura��es adicionais
spring.thymeleaf.check-template-location=true
spring.thymeleaf.enabled=true

# Endere�o da aplica��o
api.address=${APP_LINK:localhost:8080}